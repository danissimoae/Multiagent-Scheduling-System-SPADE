# Настройка системы для работы на нескольких ПК

## Архитектура

```
┌─────────────────────────────────────┐
│  ПК №1 (Сервер)                     │
│  - Openfire XMPP Server             │
│  - IP: 192.168.1.100               │
│  - Порт: 5222                       │
└─────────────────────────────────────┘
           ↑          ↑          ↑
           │          │          │
   ┌───────┘          │          └───────┐
   │                  │                  │
┌──┴──────┐    ┌──────┴─────┐    ┌──────┴─────┐
│ ПК №2   │    │  ПК №3     │    │  ПК №4     │
│ Vehicle │    │  Vehicle   │    │  Shop      │
│ Agents  │    │  Agents    │    │  Agents    │
└─────────┘    └────────────┘    └────────────┘
```

## 📋 Пошаговая инструкция

### ШАГ 1: Настройка сервера (ПК №1)

#### 1.1 Установите Openfire

1. Скачайте: https://www.igniterealtime.org/downloads/
2. Установите Openfire
3. Откройте http://localhost:9090
4. Настройте как обычно (см. WINDOWS_SETUP.md)

#### 1.2 Узнайте IP-адрес сервера

**Windows:**
```cmd
ipconfig
```

Найдите строку **IPv4 Address**, например: `192.168.1.100`

**Запишите этот IP!** Он понадобится для других компьютеров.

#### 1.3 Откройте порт в Windows Firewall

1. Откройте **Панель управления** → **Брандмауэр Windows**
2. Нажмите **Дополнительные параметры**
3. Выберите **Правила для входящих подключений** → **Создать правило**
4. Тип правила: **Для порта** → Далее
5. Протокол: **TCP**, Порты: **5222, 5269** → Далее
6. Действие: **Разрешить подключение** → Далее
7. Профиль: выберите все → Далее
8. Имя: **XMPP Server** → Готово

#### 1.4 Настройте Openfire для сетевого доступа

1. Откройте админ-панель: http://localhost:9090
2. Перейдите: **Server → Server Settings → Server Manager**
3. Убедитесь, что:
   - **Server Name**: IP вашего сервера (192.168.1.100) ИЛИ `localhost`
   - **Client Connection**: порт 5222 активен
   - **Server Connection**: порт 5269 активен

**ВАЖНО:** Если вы меняли Server Domain при установке на `localhost`, можно оставить так. Главное - использовать правильный IP в JID на клиентских ПК.

#### 1.5 Создайте пользователей

Создайте всех пользователей как обычно:
- vehicle1, vehicle2, vehicle3
- shop1, shop2, shop3

---

### ШАГ 2: Настройка клиентских ПК (ПК №2, №3, №4)

#### 2.1 Установите Python и зависимости

На каждом клиентском ПК:
```bash
pip install spade aiohttp
```

#### 2.2 Скопируйте файлы проекта

Скопируйте на каждый ПК:
- `agent.py`
- `start_distributed.py`

#### 2.3 Измените конфигурацию

В файле `start_distributed.py` измените IP сервера:

```python
# БЫЛО:
XMPP_SERVER = "localhost"

# СТАЛО (замените на IP вашего сервера):
XMPP_SERVER = "192.168.1.100"
```

**Пример итоговой конфигурации:**

```python
XMPP_SERVER = "192.168.1.100"  # IP сервера ПК №1

VEHICLES_CONFIG = [
    {
        "id": 1,
        "jid": f"vehicle1@{XMPP_SERVER}",  # vehicle1@192.168.1.100
        "password": "vehicle1pass",
        "capacity": 100,
        "speed": 60
    },
    # ...
]
```

**ВАЖНО:** JID теперь выглядит как `vehicle1@192.168.1.100` вместо `vehicle1@localhost`

---

### ШАГ 3: Запуск системы

#### На ПК №1 (сервер):
```bash
# Можно запустить автомобили или магазины, или ничего
python start_distributed.py --mode vehicles
```

#### На ПК №2:
```bash
# Например, один автомобиль
python start_distributed.py --mode vehicle --id 1
```

#### На ПК №3:
```bash
# Другой автомобиль
python start_distributed.py --mode vehicle --id 2
```

#### На ПК №4:
```bash
# Магазины
python start_distributed.py --mode shops
```

---

## 🔍 Проверка подключения

### Тест 1: Проверка доступности сервера

На клиентском ПК выполните:

```cmd
telnet 192.168.1.100 5222
```

Если подключение успешно - порт открыт

(Если команда `telnet` не найдена, включите её: **Панель управления → Программы → Включение или отключение компонентов Windows → Telnet Client**)

### Тест 2: Простой скрипт подключения

Создайте `test_network.py` на клиентском ПК:

```python
from spade.agent import Agent
import asyncio

SERVER_IP = "192.168.1.100"  # IP сервера

class TestAgent(Agent):
    async def setup(self):
        print(f" Успешно подключен к серверу {SERVER_IP}!")

async def main():
    agent = TestAgent(f"vehicle1@{SERVER_IP}", "vehicle1pass")
    await agent.start()
    await asyncio.sleep(3)
    await agent.stop()

if __name__ == "__main__":
    asyncio.run(main())
```

Запустите:
```bash
python test_network.py
```

Если увидите " Успешно подключен" - всё работает

---

## 📊 Примеры распределения агентов

### Вариант A: Все автомобили на одном ПК

```
ПК №1 (сервер): Openfire
ПК №2: все 3 автомобиля (python start_distributed.py --mode vehicles)
ПК №3: все 3 магазина (python start_distributed.py --mode shops)
```

### Вариант B: Каждый агент на своем ПК

```
ПК №1: Openfire + vehicle1
ПК №2: vehicle2
ПК №3: vehicle3
ПК №4: shop1
ПК №5: shop2 + shop3
```

### Вариант C: Смешанный

```
ПК №1 (сервер): Openfire + vehicle1 + vehicle2
ПК №2: vehicle3 + shop1
ПК №3: shop2 + shop3
```

---

## 🔧 Обновление start_distributed.py для сети

Вот обновленная версия конфигурации:

```python
import os

# Определяем IP сервера из переменной окружения или используем значение по умолчанию
XMPP_SERVER = os.getenv("XMPP_SERVER", "192.168.1.100")

# Конфигурация автомобилей с использованием переменной сервера
VEHICLES_CONFIG = [
    {
        "id": 1,
        "jid": f"vehicle1@{XMPP_SERVER}",
        "password": "vehicle1pass",
        "capacity": 100,
        "speed": 60
    },
    {
        "id": 2,
        "jid": f"vehicle2@{XMPP_SERVER}",
        "password": "vehicle2pass",
        "capacity": 150,
        "speed": 50
    },
    {
        "id": 3,
        "jid": f"vehicle3@{XMPP_SERVER}",
        "password": "vehicle3pass",
        "capacity": 80,
        "speed": 70
    }
]

# Аналогично для магазинов...
```

Теперь можно менять сервер через переменную окружения:

**Windows:**
```cmd
set XMPP_SERVER=192.168.1.100
python start_distributed.py --mode vehicles
```

**Linux/Mac:**
```bash
export XMPP_SERVER=192.168.1.100
python start_distributed.py --mode vehicles
```

---

##  Частые проблемы

### Проблема: "Connection timeout"

**Причины:**
1. Файрвол блокирует порт 5222
2. Неправильный IP сервера
3. Компьютеры в разных сетях

**Решение:**
- Проверьте файрвол на сервере
- Убедитесь, что все ПК в одной локальной сети
- Проверьте IP: `ping 192.168.1.100`

### Проблема: "Authentication failed"

**Причины:**
1. Неправильный JID формат
2. Пользователь не создан на сервере

**Решение:**
- Убедитесь, что JID формата `username@SERVER_IP`
- Проверьте, что пользователи созданы в Openfire

### Проблема: Агенты не видят друг друга

**Причины:**
1. Разные сервера в конфигурации
2. Неправильные JID

**Решение:**
- Все агенты должны использовать ОДИНАКОВЫЙ `XMPP_SERVER`
- Все JID должны быть в формате `username@ОДИНАКОВЫЙ_IP`

---

##  Запуск через интернет

Если хотите запускать агентов через интернет:

### Вариант 1: Проброс портов на роутере

1. Зайдите в настройки роутера (обычно 192.168.0.1)
2. Найдите раздел **Port Forwarding**
3. Добавьте правило:
   - External Port: 5222
   - Internal IP: IP вашего ПК (192.168.1.100)
   - Internal Port: 5222
   - Protocol: TCP
4. Узнайте ваш внешний IP: https://2ip.ru
5. Используйте внешний IP в конфигурации

### Вариант 2: VPN (проще и безопаснее)

1. Установите Hamachi или ZeroTier
2. Создайте сеть
3. Подключите все ПК к одной сети
4. Используйте VPN IP-адреса

---

##  Итоговый чеклист

- [ ] Openfire установлен на ПК №1
- [ ] Файрвол настроен (порты 5222, 5269)
- [ ] IP сервера определен (ipconfig)
- [ ] Пользователи созданы в Openfire
- [ ] На клиентских ПК установлен Python + SPADE
- [ ] В start_distributed.py указан правильный IP
- [ ] Тест подключения прошел успешно
- [ ] Агенты запущены и видят друг друга

---

Теперь ваша система работает на нескольких компьютерах в локальной сети